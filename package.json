{
  "name": "apextunnel",
  "version": "2.2.0",
  "description": "Self-hosted reverse tunnel with streaming, TLS, and rate limiting",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:app": "nodemon ./tests/test-app.js",
    "relay": "cd relayServer && pnpm dev",
    "client": "cd clientServer && pnpm dev"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemon": "^3.1.14"
  }
}
